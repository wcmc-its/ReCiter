package reciter.algorithm.evidence.author.feedback.authorcount;

import reciter.algorithm.evidence.feedback.targetauthor.AbstractTargetAuthorFeedbackStrategyContext;
import reciter.algorithm.evidence.feedback.targetauthor.TargetAuthorFeedbackStrategy;

public class AuthorCountFeedbackStrategyContext extends AbstractTargetAuthorFeedbackStrategyContext {

	public AuthorCountFeedbackStrategyContext(TargetAuthorFeedbackStrategy strategy) {
		super(strategy);
	}
}

package reciter.algorithm.evidence.article.mesh;

import reciter.algorithm.evidence.targetauthor.AbstractTargetAuthorStrategyContext;
import reciter.algorithm.evidence.targetauthor.TargetAuthorStrategy;

public class MeshMajorStrategyContext extends AbstractTargetAuthorStrategyContext {

	public MeshMajorStrategyContext(TargetAuthorStrategy strategy) {
		super(strategy);
	}
}
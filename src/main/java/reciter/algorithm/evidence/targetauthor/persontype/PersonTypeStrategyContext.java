package reciter.algorithm.evidence.targetauthor.persontype;

import reciter.algorithm.evidence.targetauthor.AbstractTargetAuthorStrategyContext;
import reciter.algorithm.evidence.targetauthor.TargetAuthorStrategy;

public class PersonTypeStrategyContext extends AbstractTargetAuthorStrategyContext {
	
	public PersonTypeStrategyContext(TargetAuthorStrategy strategy) {
		super(strategy);
	}

}

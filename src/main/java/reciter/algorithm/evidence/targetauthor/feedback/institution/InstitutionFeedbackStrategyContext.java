package reciter.algorithm.evidence.targetauthor.feedback.institution;

import reciter.algorithm.evidence.feedback.targetauthor.AbstractTargetAuthorFeedbackStrategyContext;
import reciter.algorithm.evidence.feedback.targetauthor.TargetAuthorFeedbackStrategy;

public class InstitutionFeedbackStrategyContext extends AbstractTargetAuthorFeedbackStrategyContext {

	public InstitutionFeedbackStrategyContext(TargetAuthorFeedbackStrategy strategy) {
		super(strategy);
	}
}

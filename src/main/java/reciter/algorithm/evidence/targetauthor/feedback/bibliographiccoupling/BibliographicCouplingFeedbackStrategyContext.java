package reciter.algorithm.evidence.targetauthor.feedback.bibliographiccoupling;

import reciter.algorithm.evidence.feedback.targetauthor.AbstractTargetAuthorFeedbackStrategyContext;
import reciter.algorithm.evidence.feedback.targetauthor.TargetAuthorFeedbackStrategy;

public class BibliographicCouplingFeedbackStrategyContext extends AbstractTargetAuthorFeedbackStrategyContext {

	public BibliographicCouplingFeedbackStrategyContext(TargetAuthorFeedbackStrategy strategy) {
		super(strategy);
	}
}

package reciter.algorithm.evidence.targetauthor.feedback.email;

import reciter.algorithm.evidence.feedback.targetauthor.AbstractTargetAuthorFeedbackStrategyContext;
import reciter.algorithm.evidence.feedback.targetauthor.TargetAuthorFeedbackStrategy;

public class EmailFeedbackStrategyContext extends AbstractTargetAuthorFeedbackStrategyContext {

	public EmailFeedbackStrategyContext(TargetAuthorFeedbackStrategy strategy) {
		super(strategy);
	}
}

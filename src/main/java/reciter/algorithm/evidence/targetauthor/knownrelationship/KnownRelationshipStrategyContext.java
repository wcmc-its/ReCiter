package reciter.algorithm.evidence.targetauthor.knownrelationship;

import reciter.algorithm.evidence.targetauthor.AbstractTargetAuthorStrategyContext;
import reciter.algorithm.evidence.targetauthor.TargetAuthorStrategy;

public class KnownRelationshipStrategyContext extends AbstractTargetAuthorStrategyContext {

	public KnownRelationshipStrategyContext(TargetAuthorStrategy strategy) {
		super(strategy);
	}
}
